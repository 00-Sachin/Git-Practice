# commands that  i learn 

git init : initalise your repo as a local verson controller

git status:  tells you about the change that occur

git add . : it moves the file from an file system to a staging area the "." fils allows to add all the file with a single command 

git commit -m :  commit the changes that you did in the file 

